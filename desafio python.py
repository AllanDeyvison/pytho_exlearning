
#listaNumeroImpares = [(n) for n in range(250) if n % 2 !=0 ]

#with open ("results.txt", "w") as arquivoDeNumeroImpares:
#    arquivoDeNumeroImpares.write(str(listaNumeroImpares))

#with open ("results.txt", "w") as arquivoDeNumeroImpares:
#    arquivoDeNumeroImpares.write(str([(n) for n in range(250) if n % 2 !=0 ]))

arqui = open("results2.txt", "w").write(str([(n) for n in range(250) if n % 2 ==0 ]))






#for n in range(250):
#    if n % 2 != 0:
#        listaNumeroImpares = n


#print(listaNumeroImpares)